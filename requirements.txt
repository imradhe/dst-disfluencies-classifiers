# ---------------------------------------------------------------------
# Disfluency Classifier v2 — dependencies (one-shot install)
# ---------------------------------------------------------------------
# Preferred path is ./setup.sh (creates .venv, picks the right torch
# build, verifies imports, writes requirements-lock.txt). This file is
# for a manual  `pip install -r requirements.txt`  into an existing
# Python 3.11 environment.
#
#   python3.11 -m venv .venv && . .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# GPU box: install the CUDA torch build FIRST so pip keeps it, e.g.
#   pip install torch==2.13.0 torchaudio==2.11.0 \
#       --index-url https://download.pytorch.org/cu124
#   pip install -r requirements.txt
#
# Device auto-selects cuda -> mps -> cpu in classifier/models/{dnn,bilstm}.py.
# RF (scikit-learn) is CPU-only and RAM-heavy.
# ---------------------------------------------------------------------

# deep learning (no-op if the CUDA build is already installed)
torch==2.13.0
torchaudio==2.11.0

# core stack
-r requirements-core.txt

# optional paths (prosody features; paper_repro SMOTE) — remove this
# line if they conflict on your platform; the MFCC grid does not use them
-r requirements-optional.txt
